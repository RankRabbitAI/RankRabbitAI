# Hastimal Jangid — GitHub Pages

Personal professional profile for Hastimal Jangid, Co-Founder of RankRabbit AI.

## Deploy
Enable **GitHub Pages** from repository **Settings → Pages → Deploy from branch → main → / (root)**.

Update the canonical URL in `index.html`, `robots.txt`, and `sitemap.xml` if the GitHub username/repository URL differs.
